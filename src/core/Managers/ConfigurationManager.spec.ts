import ConfigurationManager from './ConfigurationManager'


describe('ConfigurationManager', function (){
    test("Instance creation", ()=>{
        //expect(0).not.toBe(1);
    })
});

//- cette méthode, permettra aux Managers respectifs, de n'ajouter la dite instance, que lorsqu'elle est "configurée". (via isValid ou équiv)